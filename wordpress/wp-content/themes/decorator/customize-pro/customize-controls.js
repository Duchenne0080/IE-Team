( function( api ) {

	// Extends our custom "decorator" section.
	api.sectionConstructor['decorator'] = api.Section.extend( {

		// No events for this type of section.
		attachEvents: function () {},

		// Always make the section active.
		isContextuallyActive: function () {
			return true;
		}
	} );

} )( wp.customize );